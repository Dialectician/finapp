.loader
  z-index 100
  position fixed
  display flex
  align-items center
  justify-content center
  flex-flow column wrap
  left 0
  top 0
  width 100%
  height 100%
  background var(--main-bg)

  &__wrap
    display flex
    flex-flow column nowrap
    align-items center
